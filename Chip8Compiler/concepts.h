#pragma once

#include "common.h"

namespace Chip8Compiler
{
	/*
	class Statement;

	namespace Statements
	{
		class FunctionCall;
		class Number;
		class VariableCall;
	}

	namespace Concepts
	{
		// can't use this for now, we'll see later
		template<typename T>
		concept FunctionArgument
			= std::is_same<T, Statements::FunctionCall>::value
			|| std::is_same<T, Statements::VariableCall>::value
			|| std::is_same<T, Statements::Number>::value;
	}
	*/
}